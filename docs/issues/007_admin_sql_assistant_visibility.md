---
id: "007"
title: "Fix Admin SQL Assistant Contrast, Scrolling, and Formatting"
status: "closed"
labels: ["ready-for-agent"]
blocked_by: []
---

> *This was generated by AI during triage.*

## What to build

Fix the UI/UX bugs in the Admin SQL Assistant component:
1. Replace all occurrences of the invalid Tailwind color class `bg-slate-955` with valid theme colors (`bg-slate-900` or `bg-slate-950`).
2. Implement auto-scrolling to the bottom of the messages viewport whenever a message is added, the loading state triggers, or assistant response completes.
3. Add markdown parsing support to properly format lists, bold text, and line spacing for the text summaries returned by the assistant.

## Acceptance criteria

- [ ] All occurrences of the invalid class `bg-slate-955` are replaced with `bg-slate-900` or `bg-slate-950`.
- [ ] The conversation area automatically scrolls to the bottom when a new user message is sent, a thinking loader appears, or an assistant message completes.
- [ ] Markdown content (e.g., bulleted lists, bold text) in assistant summaries renders cleanly instead of showing raw markdown characters.

## Blocked by

None - can start immediately
